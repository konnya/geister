using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameSceneOld : MonoBehaviour {

	// Use this for initialization
	void Start () {
		// Instantiate(GhostBlue);
		GameObject obj = Resources.Load("GhostBlue") as GameObject;
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
